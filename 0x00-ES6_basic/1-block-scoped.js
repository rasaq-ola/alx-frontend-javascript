function taskBlock(trueOrFalse) {
    if (trueOrFalse) {
        const task = true;  
        const task2 = false; 
    }

    return [task, task2];  // Adjust as needed if you still want to return these
}
